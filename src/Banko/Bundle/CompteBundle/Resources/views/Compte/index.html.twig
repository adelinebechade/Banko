{% extends "BankoCompteBundle::layout.html.twig" %}
 
{% block title %}
  Accueil - {{ parent() }}
{% endblock %}

{% block banko_body %}
 
  <h2><a href="{{ path('banko_home') }}">Liste des comptes</a></li></h2>
 
  <table class="table table-hover table-bordered table-condensed">
    <thead>
        <tr>
            <th>Nom</th>
            <th>Solde Courant</th>
            <th>Solde Prévisionnel</th>
        </tr>
    </thead>
    {% for compte in liste_comptes %}
      <tr>
        <td><a href="{{ path('banko_voir', {'id': compte.id, 'page': 1}) }}">{{ compte.nom }}</a></td>
        <td><a href="{{ path('banko_voir', {'id': compte.id, 'page': 1}) }}">{{ solde_courant[compte.id] }}</a></td>
        <td><a href="{{ path('banko_voir', {'id': compte.id, 'page': 1}) }}">{{ solde_previsionnel[compte.id] }}</a></td>
      </tr>
    {% else %}
      <tr>Pas (encore !) de compte</tr>
    {% endfor %}
  </table>

{% endblock %}